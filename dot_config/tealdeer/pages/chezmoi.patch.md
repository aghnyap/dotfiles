- This repo's own alias (dot_config/zsh/functions.zsh) for `chezmoi diff`:

`cmd`

- This repo's own alias for `chezmoi re-add`:

`cmr`

- This repo's own alias for `chezmoi add`:

`cma`

- This repo's own alias for `chezmoi edit --apply` (open a managed file's source, then apply it):

`cme`

- This repo's own alias for `chezmoi cd`:

`cmcd`

- Full command reference for this repo's chezmoi workflow:

`tldr dotfiles-chezmoi`
