#!/usr/bin/env zsh
# $BROWSER entry point: Arc via macOS `open`, explicit and not
# dependent on the OS-level default browser (this repo doesn't manage that
# setting, and doesn't install Arc itself).
set -euo pipefail
exec open -a 'Arc' "$@"
